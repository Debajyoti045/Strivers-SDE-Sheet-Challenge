#include <bits/stdc++.h> 
int atoi(string str) {
    int sign = 1;
    if(str[0]=='-') sign = -1;
    int ans = 0;
    for(int i=0;i<str.size();i++){
        if(str[i]>='0' && str[i]<='9') {
            ans = ans * 10 + (str[i]-'0');
        }
    }
    return sign * ans;
}
